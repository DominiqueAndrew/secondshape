[project]
name = "secondshape"
version = "1.0.0"
description = "Let reclaimed material reshape the brief."
requires-python = ">=3.11"
dependencies = ["Flask==3.1.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
